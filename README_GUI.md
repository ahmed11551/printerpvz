# Графический интерфейс (GUI)

## Что это?

Простая программа с графическим интерфейсом, которая объединяет все функции:
- Запуск/остановка сервера одной кнопкой
- Просмотр статуса и статистики
- История печати
- Установка расширения браузера
- Открытие веб-панели
- Просмотр логов

## Запуск

### Windows:
```
Двойной клик на ЗАПУСТИТЬ_GUI.bat
```

### Mac/Linux:
```bash
chmod +x ЗАПУСТИТЬ_GUI.sh
./ЗАПУСТИТЬ_GUI.sh
```

Или напрямую:
```bash
python3 app_gui.py
```

## Возможности

### 1. Управление сервером
- Кнопка "▶ Запустить сервер" - запускает сервер одной кнопкой
- Индикатор статуса (зеленый/красный кружок)
- Автоматическое обновление статуса каждые 2 секунды

### 2. Статистика
- Всего напечатано
- Успешных печатей
- Ошибок
- Печатей за сегодня

### 3. История печати
- Таблица последних 20 печатей
- Время, номер ячейки, статус (✓/✗)
- Кнопка обновления

### 4. Установка расширения
- Кнопка "🌐 Установить расширение"
- Показывает пошаговую инструкцию
- Автоматически открывает папку расширения
- Открывает страницы расширений в браузерах

### 5. Веб-панель
- Кнопка "📊 Веб-панель" открывает http://localhost:5001/
- Доступ ко всем функциям веб-интерфейса

### 6. Логи
- Окно с логами внизу
- Показывает информацию о работе сервера
- Автоматическая прокрутка к последним записям

## Преимущества GUI версии

✅ **Простота:** Все в одном окне
✅ **Наглядность:** Статус и статистика видны сразу
✅ **Удобство:** Не нужно открывать браузер для базовых операций
✅ **История:** Быстрый доступ к последним печатям
✅ **Логи:** Видно что происходит в реальном времени

## Сравнение с веб-версией

| Функция | GUI | Веб-панель |
|---------|-----|------------|
| Запуск сервера | ✅ | ❌ (нужно запускать отдельно) |
| Статус в реальном времени | ✅ | ✅ |
| Статистика | ✅ | ✅ |
| История | ✅ (20 записей) | ✅ (неограниченно) |
| Настройки принтера | 🔗 (открывает веб) | ✅ |
| Установка расширения | ✅ (помощник) | ❌ |
| Логи | ✅ | ❌ |

## Требования

- Python 3.7+
- Все зависимости из requirements.txt (устанавливаются автоматически)
- tkinter (обычно встроен в Python)

Если tkinter не установлен:

**Windows:** Обычно встроен

**Ubuntu/Debian:**
```bash
sudo apt-get install python3-tk
```

**macOS:** Обычно встроен

**Fedora/RHEL:**
```bash
sudo dnf install python3-tkinter
```

