# Инструкция по установке

## Шаг 1: Установка Python и зависимостей

### Windows:
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Откройте командную строку (cmd) и выполните:
   ```cmd
   pip install -r requirements.txt
   ```

### macOS:
```bash
# Установите Python через Homebrew (если еще не установлен)
brew install python3

# Установите зависимости
pip3 install -r requirements.txt

# Установите libusb для USB принтеров
brew install libusb
```

### Linux (Ubuntu/Debian):
```bash
# Установите Python и pip
sudo apt-get update
sudo apt-get install python3 python3-pip

# Установите зависимости
pip3 install -r requirements.txt

# Установите libusb для USB принтеров
sudo apt-get install libusb-1.0-0-dev

# Добавьте пользователя в группы для доступа к принтерам
sudo usermod -a -G dialout $USER
sudo usermod -a -G plugdev $USER
# Перезайдите в систему после этого
```

## Шаг 2: Определение порта принтера

### Windows:
1. Откройте "Диспетчер устройств"
2. Найдите ваш принтер в разделе "Порты (COM и LPT)"
3. Запомните номер COM-порта (например, COM3, COM4)

### macOS:
Откройте терминал и выполните:
```bash
ls /dev/tty.usbserial* /dev/tty.usbmodem* /dev/cu.* 2>/dev/null
```
Вы увидите список доступных портов.

### Linux:
```bash
ls /dev/ttyUSB* /dev/ttyACM* /dev/ttyS*
```

Для USB принтеров определите vendor_id и product_id:
```bash
lsusb
```
Найдите строку с вашим принтером, например:
```
Bus 001 Device 003: ID 04f9:2042 Epson TM-T20III
```
Здесь `04f9` (hex) = 1273 (decimal) - vendor_id, `2042` (hex) = 8258 (decimal) - product_id

## Шаг 3: Настройка config.json

Откройте файл `config.json` и измените настройки:

### Для Serial принтера (COM-порт):
```json
{
  "type": "serial",
  "serial_port": "COM3",     // Замените на ваш порт
  "serial_baudrate": 9600,   // Обычно 9600 или 115200
  "width": 58
}
```

### Для USB принтера:
```json
{
  "type": "usb",
  "vendor_id": 1273,         // Из lsusb (десятичное число)
  "product_id": 8258,        // Из lsusb (десятичное число)
  "width": 58
}
```

### Для сетевого принтера:
```json
{
  "type": "network",
  "network_host": "192.168.1.100",  // IP адрес принтера
  "network_port": 9100,              // Обычно 9100
  "width": 58
}
```

## Шаг 4: Запуск сервера печати

### Windows:
Дважды кликните на `start_server.bat` или в командной строке:
```cmd
python server.py
```

### macOS/Linux:
```bash
chmod +x start_server.sh
./start_server.sh
```

Или напрямую:
```bash
python3 server.py
```

Вы должны увидеть сообщение:
```
Загрузка конфигурации...
Инициализация принтера...
Принтер инициализирован: serial
Запуск сервера на http://localhost:5000
```

**Важно:** Не закрывайте это окно, сервер должен работать постоянно.

## Шаг 5: Установка браузерного расширения

### Chrome:
1. Откройте Chrome
2. Введите в адресной строке: `chrome://extensions/`
3. Включите "Режим разработчика" (переключатель справа вверху)
4. Нажмите "Загрузить распакованное расширение"
5. Выберите папку с расширением (папка где находится `manifest.json`)
6. Расширение появится в списке

### Microsoft Edge:
1. Откройте Edge
2. Введите в адресной строке: `edge://extensions/`
3. Включите "Режим разработчика" (переключатель слева)
4. Нажмите "Загрузить распакованное расширение"
5. Выберите папку с расширением

### Яндекс.Браузер:
1. Откройте `browser://extensions/`
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку

## Шаг 6: Проверка работы

1. Убедитесь, что сервер печати запущен
2. Откройте popup расширения (кликните на иконку расширения)
3. Введите тестовый номер ячейки (например, "A1-1")
4. Нажмите "Печать тестовой ячейки"
5. Принтер должен напечатать этикетку

Если печать не работает, проверьте:
- Запущен ли сервер печати
- Правильно ли указан порт в config.json
- Подключен ли принтер
- Есть ли бумага в принтере

## Шаг 7: Использование

1. Откройте страницу приемки в Турбо-ПВЗ (Ozon) или ВВПВЗ (Wildberries)
2. При сканировании заказа и присвоении ячейки, она автоматически отправится на печать
3. На экране появится уведомление "Ячейка X отправлена на печать"

## Решение проблем

### Ошибка "Принтер не инициализирован"
- Проверьте подключение принтера
- Проверьте правильность порта/IP в config.json
- Попробуйте перезапустить сервер

### Ошибка "Permission denied" (Linux/Mac)
- Добавьте пользователя в группы: `sudo usermod -a -G dialout,plugdev $USER`
- Перезайдите в систему

### Ошибка подключения расширения к серверу
- Убедитесь, что сервер запущен на localhost:5000
- Проверьте настройки URL в popup расширения
- Попробуйте отключить файрвол

### Принтер печатает мусор
- Проверьте скорость (baudrate) в config.json
- Попробуйте изменить на 115200 или 9600
- Убедитесь, что принтер поддерживает ESC/POS

### Ячейка не определяется автоматически
- Откройте консоль браузера (F12) и проверьте ошибки
- Попробуйте вручную ввести номер ячейки в popup расширения
- Убедитесь, что расширение активировано на нужном сайте

## Готово! 🎉

Теперь система автоматически будет печатать номера ячеек при приемке заказов.

