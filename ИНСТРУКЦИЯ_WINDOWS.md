# Полная инструкция по запуску на Windows

## Шаг 1: Установка Python

### Проверка наличия Python

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. В командной строке введите:
   ```
   python --version
   ```
4. Если появилась версия (например, `Python 3.9.6`) — Python установлен, переходите к Шагу 2
5. Если появилась ошибка `'python' не является внутренней или внешней командой` — нужно установить Python

### Установка Python (если не установлен)

1. Откройте браузер и перейдите на https://www.python.org/downloads/
2. Нажмите большую желтую кнопку **"Download Python"** (загрузится последняя версия)
3. Запустите скачанный файл установки (например, `python-3.11.x.exe`)
4. **ВАЖНО:** В окне установки обязательно отметьте галочку:
   - ☑ **"Add Python to PATH"** (внизу окна)
5. Нажмите **"Install Now"**
6. Дождитесь завершения установки
7. Нажмите **"Close"**
8. **Перезагрузите компьютер** (важно для применения изменений PATH)

### Проверка установки Python

1. Снова откройте командную строку (`Win + R` → `cmd`)
2. Введите:
   ```
   python --version
   ```
3. Должна появиться версия Python (например, `Python 3.11.0`)
4. Если всё хорошо — переходите к следующему шагу

---

## Шаг 2: Установка программы

1. Скачайте или распакуйте папку с программой на диск (например, `C:\Печать_этикеток\`)
2. Откройте папку с программой
3. Найдите файл **`УСТАНОВКА.bat`**
4. **Двойной клик** на файл `УСТАНОВКА.bat`
5. Дождитесь завершения установки (установятся необходимые библиотеки)
6. Если появилось сообщение **"УСТАНОВКА ЗАВЕРШЕНА"** — всё готово!

### Что делает установка:
- Проверяет наличие Python
- Устанавливает необходимые библиотеки (Flask, pyserial, python-escpos и др.)
- Создает скрипты для запуска

**Если возникла ошибка:**
- Убедитесь, что Python установлен и добавлен в PATH
- Запустите установку от имени администратора (правой кнопкой → "Запуск от имени администратора")

---

## Шаг 3: Настройка принтера

1. Откройте папку с программой
2. Найдите файл **`config.json`**
3. Откройте его в **Блокноте** (правой кнопкой → "Открыть с помощью" → "Блокнот")

### Определение порта принтера

**Способ 1: Через Диспетчер устройств**
1. Нажмите `Win + X`
2. Выберите **"Диспетчер устройств"**
3. Найдите раздел **"Порты (COM и LPT)"**
4. Найдите ваш принтер (например, `USB Serial Port (COM3)`)
5. Запомните номер COM-порта (например, `COM3`)

**Способ 2: Через веб-интерфейс программы**
1. Запустите сервер (см. Шаг 4)
2. Откройте браузер и перейдите на http://localhost:5001/settings
3. Нажмите кнопку "Сканировать порты"
4. Выберите нужный порт из списка

### Настройка config.json

**Для Serial принтера (через COM-порт):**
```json
{
  "type": "serial",
  "serial_port": "COM3",
  "serial_baudrate": 9600
}
```
Замените `COM3` на номер вашего COM-порта (например, COM4, COM5 и т.д.)

**Для USB принтера:**
```json
{
  "type": "usb",
  "vendor_id": 1200,
  "product_id": 8258
}
```
Замените `vendor_id` и `product_id` на значения вашего принтера (можно найти в документации принтера)

**Для сетевого принтера:**
```json
{
  "type": "network",
  "network_host": "192.168.1.100",
  "network_port": 9100
}
```
Замените `192.168.1.100` на IP-адрес вашего принтера

4. Сохраните файл (`Ctrl + S`)

---

## Шаг 4: Запуск сервера

**Важно:** Сервер нужно запускать каждый раз после включения компьютера, но можно настроить **автозапуск** (см. раздел "Автозапуск сервера" ниже).

### Вариант А: Запуск через GUI (рекомендуется)

1. Откройте папку с программой
2. Найдите файл **`ЗАПУСТИТЬ_GUI.bat`**
3. **Двойной клик** на файл
4. Откроется окно программы с кнопками управления
5. Нажмите кнопку **"▶ Запустить сервер"**
6. Должен появиться зеленый индикатор и статус "Сервер: Работает"

**Преимущества:** Можно видеть статус, статистику, историю печати в одном окне.

### Вариант Б: Запуск через консоль

1. Откройте папку с программой
2. Найдите файл **`ЗАПУСТИТЬ.bat`**
3. **Двойной клик** на файл
4. Откроется окно командной строки
5. Должно появиться сообщение:
   ```
   Запуск сервера на http://localhost:5001
   * Running on http://127.0.0.1:5001
   ```
6. **Не закрывайте это окно!** Сервер работает пока окно открыто

### Вариант В: Запуск в фоне (без окна)

1. Найдите файл **`ЗАПУСТИТЬ_ФОНОМ.bat`**
2. **Двойной клик** на файл
3. Сервер запустится в фоне (окно быстро закроется)
4. Проверьте работу: откройте http://localhost:5001 в браузере

**Проверка работы сервера:**
- Откройте браузер
- Перейдите на http://localhost:5001
- Должна открыться страница "Сервер печати ячеек ПВЗ"

---

## Шаг 5: Установка расширения браузера

### Способ 1: Автоматическая установка (рекомендуется)

1. В папке с программой найдите файл **`УСТАНОВИТЬ_РАСШИРЕНИЕ.bat`**
2. **Двойной клик** на файл
3. Автоматически откроются страницы расширений в браузерах
4. Следуйте инструкциям на экране

### Способ 2: Ручная установка в Chrome/Edge

1. Откройте Chrome или Edge
2. В адресной строке введите: `chrome://extensions/` (для Chrome) или `edge://extensions/` (для Edge)
3. Нажмите Enter
4. В правом верхнем углу включите **"Режим разработчика"** (переключатель)
5. Нажмите кнопку **"Загрузить распакованное расширение"**
6. Найдите и выберите папку **`extension`** внутри папки с программой
   - Например: `C:\Печать_этикеток\extension`
7. Нажмите **"Выбрать папку"**
8. Расширение должно появиться в списке

### Способ 3: Ручная установка в Яндекс.Браузере

1. Откройте Яндекс.Браузер
2. В адресной строке введите: `browser://extensions/`
3. Нажмите Enter
4. Или откройте меню (три горизонтальные линии) → **Дополнения** → **Управление дополнениями**
5. В правом верхнем углу включите **"Режим разработчика"** (переключатель)
6. Нажмите кнопку **"Загрузить распакованное расширение"**
7. Найдите и выберите папку **`extension`**
8. Нажмите **"Выбрать папку"**
9. Расширение установлено!

**Проверка установки:**
- В правом верхнем углу браузера должна появиться иконка расширения
- Кликните на иконку — должно открыться окно настроек

---

## Шаг 6: Первое использование

1. **Убедитесь, что сервер запущен** (окно с сервером открыто или GUI показывает "Сервер: Работает")
2. Откройте сайт Ozon (https://seller.ozon.ru) или Wildberries (https://seller.wildberries.ru)
3. Перейдите на страницу приемки товаров
4. Сканируйте товар сканером штрих-кодов
5. Система автоматически обнаружит номер ячейки и отправит на печать
6. Проверьте принтер — должна напечататься этикетка

### Тестовая печать

Если хотите проверить работу без сканирования:

1. Кликните на иконку расширения в браузере
2. В поле "Тестовая ячейка" введите номер (например, `122` или `A1-1`)
3. Нажмите **"Печать тестовой ячейки"**
4. Проверьте принтер — должна напечататься этикетка

---

## Решение проблем

### Проблема: "Python не найден" при установке

**Решение:**
1. Переустановите Python
2. При установке обязательно отметьте **"Add Python to PATH"**
3. Перезагрузите компьютер
4. Попробуйте снова

### Проблема: Сервер не запускается

**Решение:**
1. Проверьте, установлен ли Python: `python --version` в командной строке
2. Переустановите зависимости: запустите `УСТАНОВКА.bat` еще раз
3. Проверьте, не занят ли порт 5001 (закройте другие программы, использующие этот порт)

### Проблема: Принтер не печатает

**Решение:**
1. Проверьте настройки в `config.json` (правильный ли COM-порт указан)
2. Убедитесь, что принтер подключен и включен
3. Проверьте веб-интерфейс: http://localhost:5001 (должен показывать статус принтера)
4. Попробуйте тестовую печать через popup расширения

### Проблема: Расширение не устанавливается

**Решение:**
1. Убедитесь, что вы выбрали именно папку `extension`, а не файлы внутри неё
2. Проверьте, что включен "Режим разработчика"
3. Попробуйте перезапустить браузер

### Проблема: Ячейки не обнаруживаются

**Решение:**
1. Убедитесь, что расширение установлено и включено
2. Проверьте, что вы находитесь на сайте Ozon или Wildberries
3. Обновите страницу (F5)
4. Проверьте консоль браузера (F12 → Console) на наличие ошибок

---

## Дополнительные возможности

### Веб-панель управления

Откройте в браузере: http://localhost:5001

**Доступные функции:**
- Просмотр статуса сервера и принтера
- Статистика печати
- История печати
- Настройки принтера
- Список доступных COM-портов

### Автозапуск сервера при включении компьютера

**Способ 1: Автоматическая настройка (рекомендуется)**

1. Найдите файл **`АВТОЗАПУСК_СЕРВЕРА.bat`** в папке с программой
2. Двойной клик на файл
3. Выберите:
   - **[1]** - для GUI версии (с окном управления)
   - **[2]** - для консольной версии (только сервер)
4. Готово! Сервер будет запускаться автоматически при включении компьютера

**Способ 2: Ручная настройка**

1. Нажмите `Win + R`
2. Введите `shell:startup` и нажмите Enter
3. Скопируйте файл `ЗАПУСТИТЬ.bat` (или `ЗАПУСТИТЬ_GUI.bat`) в открывшуюся папку
4. Сервер будет запускаться автоматически при включении компьютера

**Отключение автозапуска:**

1. Запустите `АВТОЗАПУСК_СЕРВЕРА.bat`
2. Выберите **[3]** - Отключить автозапуск

**Проверка статуса:**

1. Запустите `АВТОЗАПУСК_СЕРВЕРА.bat`
2. Выберите **[4]** - Проверить статус

### Создание EXE-файла (без установки Python)

Если хотите создать версию программы, которая не требует установки Python:

1. Запустите `СОЗДАТЬ_EXE.bat`
2. Дождитесь завершения (может занять несколько минут)
3. EXE-файл будет в папке `dist`
4. Скопируйте папку `dist` на другой компьютер
5. Запустите `Печать_Ячеек_ПВЗ.exe` или `ЗАПУСТИТЬ_EXE.bat`

**Важно:** Расширение браузера всё равно нужно установить отдельно!

---

## Быстрый старт (краткая версия)

1. ✅ Установить Python (https://www.python.org/downloads/) — отметить "Add Python to PATH"
2. ✅ Запустить `УСТАНОВКА.bat`
3. ✅ Настроить `config.json` (указать COM-порт принтера)
4. ✅ Запустить `ЗАПУСТИТЬ_GUI.bat` → нажать "Запустить сервер"
5. ✅ Запустить `УСТАНОВИТЬ_РАСШИРЕНИЕ.bat` → установить расширение
6. ✅ Открыть Ozon/Wildberries → сканировать товары → автоматическая печать!

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи сервера в папке `logs/`
3. Проверьте консоль браузера (F12) на наличие ошибок

**Успешной работы!** 🖨️

